// import {auth} from '../firebase'

const Profile = () => {
    return (
        <>
    <div>This is Blank Prodile Page</div>;
    {/* <button onClick={()=>auth.signOut()}>Logout</button> */}
        </>
    )

};

export default Profile;